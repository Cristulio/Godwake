# Dead / Stale Code Audit — 2026-05-31

> **READ-ONLY audit.** No src/ files were changed. All grep evidence listed;
> severity is DEAD-safe-delete / SUSPICIOUS / STALE-needs-rewrite.

---

## Top 5 Highest-Value Cleanups

| # | Item | Severity | Why it matters |
|---|------|----------|----------------|
| 1 | `scripts/sim-economy.ts` + `sim-difficulty-curve.ts` + 17 other scripts | STALE-needs-rewrite | Model 3-class/4-chapter game. Running them now prints ~100% death everywhere — the state doc already calls them untrustworthy. Engineers treating any output as truth would make wrong balance calls. |
| 2 | `EnemyCard.tsx` | DEAD-safe-delete | 166 lines with zero callers. BattlefieldSprite.tsx absorbed its damage-float + hit-flash logic. Carrying it creates noise for anyone exploring the combat component tree. |
| 3 | `CharacterCreationScreen.v1.tsx` | DEAD-safe-delete | 400+ lines; zero imports anywhere. The point-buy flow was superseded by the fixed-stat class-selection screen and never removed. |
| 4 | `sharpen` / `prayer` camp-choice paths | SUSPICIOUS | The delveStore action, the type union, the two character fields it writes (`delveSpellAttackBonus` / `delveAttackBonus` via camp), and the CampRoom import of `pickCampChoice` all exist — but the only UI caller ever dispatches `'rest'`. Dead store branch that adds schema surface area. |
| 5 | `requiresCha` event field + code paths | SUSPICIOUS | Schema field, `EventRoom.tsx` label branch, and `applyEventOutcome.ts` gate all exist. Zero events in `src/content/events/index.ts` set it (grep: 0 matches). `skillCheck` is the live replacement. |

---

## Section A — Dead Components

### A1. `src/components/combat/EnemyCard.tsx`

| field | value |
|-------|-------|
| **File** | `src/components/combat/EnemyCard.tsx:153` |
| **What** | `EnemyCard` memo component — per-monster HP bar, hit-flash, damage floats, crit sparkle |
| **Evidence** | `grep -r "import.*EnemyCard\|<EnemyCard" src/ --include="*.tsx" --include="*.ts"` → **zero results** outside the file itself. Memory note confirms: "EnemyCard.tsx is dead." |
| **Severity** | **DEAD-safe-delete** |
| **Recommendation** | Delete the file. `BattlefieldSprite.tsx:7` already imports `resolveSpriteFloat` from `FloatingDamage.tsx` and renders the same juice. |

### A2. `src/components/creation/CharacterCreationScreen.v1.tsx`

| field | value |
|-------|-------|
| **File** | `src/components/creation/CharacterCreationScreen.v1.tsx:130` |
| **What** | Old point-buy character-creation screen (standard-array assign flow, race bonuses, `SIR_BRICK_PRESET` fill button) |
| **Evidence** | `grep -r "CharacterCreationScreen.v1" src/ --include="*.ts" --include="*.tsx"` → **zero results**. The current `CharacterCreationScreen.tsx` is a fixed-stat class-picker. |
| **Severity** | **DEAD-safe-delete** |
| **Recommendation** | Delete file. `SIR_BRICK_PRESET` stays alive (used in `scripts/sim-*.ts` and `src/sim/crossFeatureStressSim.ts`). |

---

## Section B — Dead / Vestigial Store Paths

### B1. `sharpen` and `prayer` camp-choice paths

| field | value |
|-------|-------|
| **Files** | `src/stores/delveStore.ts:716-745`, `src/stores/gameStore.ts:179,545-546`, `src/types/delve.ts:84` |
| **What** | `pickCampChoice` accepts `'rest' \| 'sharpen' \| 'prayer'`. The `sharpen` branch bumps `delveAttackBonus`/`delveSpellAttackBonus`; the `prayer` branch grants a blessing. Both are fully implemented in delveStore. |
| **Evidence** | `grep -n "pickCampChoice" src/components/delve/CampRoom.tsx` → only `pickCampChoice('rest')` (line 123). The `sharpen` and `prayer` strings appear nowhere else in `src/components/`. |
| **Severity** | **SUSPICIOUS** — the store paths work; the UI just never offers them. `delveAttackBonus` IS read by `playerAttack.ts:122`; `delveSpellAttackBonus` IS read by `spells/helpers.ts:50`. Events can also grant these via `applyEventOutcome.ts:420,424`. The camp *path specifically* is dead, not the fields themselves. |
| **Recommendation** | Either remove `'sharpen' \| 'prayer'` from the type union + store action (since the UI won't dispatch them), or re-expose them in CampRoom's rest-fork UI if the design intent was to bring them back later. |

### B2. `attunement: false` item field — dead code branch in tooltip

| field | value |
|-------|-------|
| **File** | `src/components/inventory/ItemTooltip.tsx:133` |
| **What** | `if (item.kind !== 'consumable' && item.attunement)` — intended to show an "Attunement required" badge |
| **Evidence** | Every item definition in `src/content/items/` (weapons.ts, armor.ts, robes.ts, accessories.ts) has `attunement: false`. The condition is structurally unreachable. |
| **Severity** | **SUSPICIOUS** — the feature was probably planned but never populated. The `attunement` field is part of the Item schema (`src/schemas/item.ts`). |
| **Recommendation** | Strip the `attunement` field from all item definitions + the schema + the dead tooltip branch, or add at least one `attunement: true` item to make the branch live. |

---

## Section C — Dead Event-Schema Fields

### C1. `requiresCha` — event choice gate, dead in content

| field | value |
|-------|-------|
| **Files** | `src/schemas/event.ts:86`, `src/engine/delve/applyEventOutcome.ts:68-79`, `src/components/delve/EventRoom.tsx:170-171` |
| **What** | `requiresCha: z.number().optional()` — BG2-style CHA floor gate. Engine checks `modifierFor(character, 'cha') < choice.requiresCha` and blocks the choice. UI renders `CHA +N` hint. |
| **Evidence** | `grep -n "requiresCha" src/content/events/index.ts` → **0 results**. `grep -rn "requiresCha" src/content/` → **0 results**. The events-polish lane (#174) replaced all old CHA gates with `skillCheck: { skill: 'persuasion', dc: N }`. |
| **Severity** | **SUSPICIOUS** — the schema field, the `canTakeChoice` gate, and the UI hint branch are all unreachable for any event in the current pool. |
| **Recommendation** | Remove `requiresCha` from `EventChoiceSchema`, the gate in `canTakeChoice`, and the UI hint. If a future event needs a hard CHA floor (not a roll), re-introduce it then. |

---

## Section D — Stale Simulation Scripts (STALE-needs-rewrite)

All scripts below model the old **3-class (Rogue / Fighter / Wizard) × 4-chapter (37-room)** run.
Barbarian and Ranger joined in round-2 (#163); the game is now a **6-chapter, ~103-node** branching map.
Running these scripts produces 100% death rates (see state doc: "Sims DEFERRED — loot-blind fidelity gap").
None are imported by `src/` production code; several have companion `.test.ts` files in `scripts/` that run as part of `npm test`.

| Script | Stale markers | Notes |
|--------|--------------|-------|
| `scripts/sim-economy.ts` | Ch1→Ch4, 37 rooms, 3 classes, no gear/loot | State doc explicitly calls it untrustworthy; models boss IDs from old chapter ordering |
| `scripts/sim-economy.test.ts` | 3 classes | Has a live smoke test (`runOnce` 1 fighter L1); gated heavy tests behind `RUN_SIM=1` |
| `scripts/sim-difficulty-curve.ts` | Ch1→Ch4, 3 classes | 100% death everywhere per state doc |
| `scripts/sim-full-matrix.ts` | Ch1→Ch4, 37 rooms, 3 classes | The "Phase 1 baseline" — superseded by sim-class-viability.ts |
| `scripts/sim-class-identity.ts` | Ch1→Ch4, 3 classes | Damage-source split for R/F/W only; Ch1-Ch4 chapter columns |
| `scripts/sim-class-tour-early.ts` | 3 classes | Ch1+Ch2 tour only |
| `scripts/sim-class-tour-late.ts` | Ch4 pool imports, 3 classes | Imports `EARLY_MID_POOL as CH4_EM` from chapter2Pools — wrong chapter |
| `scripts/sim-boss-intel-roi.ts` | Ch4 pool imports, 3 classes | Imports `chapter4Pools.EARLY_MID_POOL` |
| `scripts/sim-camp-boons.ts` | Ch1→Ch4, 3 classes | Camp-boon lift measured over old 4-chapter structure |
| `scripts/sim-choice-impact.ts` | 3 classes | Race×class + blessing delta; no Barb/Ranger |
| `scripts/sim-fighter.ts` | Ch1-Ch4, 3-class harness | Fighter-specific deep-dive; old chapter boss IDs |
| `scripts/sim-wizard.ts` | Ch4 pool imports, 3 classes | Imports `chapter4Pools.EARLY_MID_POOL as CH4_EM` |
| `scripts/sim-validate-wizard-rework.ts` | Ch4 pool imports, 3 classes | Pre-robes/detank wizard; imports `chapter4Pools.EARLY_MID_POOL as CH4_EM` |
| `scripts/sim-reincarnation-loop.ts` | 3 classes | Reincarnation loop across 3 classes only |
| `scripts/sim-blessing-fidelity.ts` | 3 classes | Blessing-picking bot validation; no Barb/Ranger |
| `scripts/sim-blessing-diversity.ts` | 3 classes | Per-blessing lift vs control; no Barb/Ranger |
| `scripts/sim-blessing-diversity-diagnose.ts` | 3 classes | Downstream diagnostic reading diversity snapshot |
| `scripts/sim-enemy-tuning.ts` | 3 classes | Wave-2 enemy mechanic tuning; no Barb/Ranger |
| `scripts/sim-events.ts` | 3 classes | Event-mechanic audit; CHA-gate section now moot (zero events use `requiresCha`) |
| `scripts/sim-event-flow.ts` | 3 classes | Event-flow validation; still tests valid `skillCheck` paths |
| `scripts/sim-foyer-spotcheck.ts` | 3 classes | Spot-checks Ch2 Counting House Foyer; room still exists but no Barb/Ranger |

**Fresh scripts (not stale):**

| Script | Why current |
|--------|-------------|
| `scripts/sim-class-viability.ts` | 5 classes, 6-chapter aware (mentions "old 4-chapter chain" only as historical context) |
| `scripts/sim-feel.ts` | 5 classes, 6-chapter aware |
| `scripts/sim-endgame-gear.ts` | 5 classes, Ch5/Ch6 gate modeling, gear/legendary loop |

---

## Section E — Stale src/sim Harnesses

| File | Stale markers | Notes |
|------|--------------|-------|
| `src/sim/rogueSim.ts` / `.test.ts` | Pre-Barb/Ranger; 4-chapter harness | Was the "rogue immortal" diagnostic; superseded by sim-class-viability.ts policy |
| `src/sim/immortalHypothesisSim.ts` / `.test.ts` | 3 classes, old loadout combos | PR #80/#86 aggregator fix validation — fix is shipped; harness is historical |
| `src/sim/crossFeatureStressSim.ts` / `.test.ts` | "4 chapters, 58 rooms" hardcoded | Phase-1 8-feature stress test; still compiles but models old chapter structure |
| `src/sim/stackingAggregatorSim.test.ts` | 3 classes | Stacking-aggregator static checks still valid; sim section models old chapter structure |

---

## Section F — Disabled Content (non-pool)

### F1. Disabled quirks (6 entries)

| Quirk id | Quirk name |
|----------|-----------|
| `sun-touched` | Sun-Touched |
| `bardic-memory` | Bardic Memory |
| `silver-tongue` | Silver Tongue |
| `plain-face` | Plain Face |
| `cold-touched` | Cold-Touched |
| `beast-marked` | Beast-Marked |

**Evidence:** `grep -n "enabled: false" src/content/quirks/index.ts` → 6 blocks. `POOL.filter(q => q.enabled !== false)` at line 263 excludes them.
**Severity:** SUSPICIOUS — design intent (unfinished mechanic vs. intentional disable) is unclear from code alone. The filter correctly excludes them from the pool. Low risk, low urgency.
**Recommendation:** Delete disabled entries that have no planned implementation, or add a TODO comment with rationale.

---

## Section G — Migration Artifacts (informational)

These appear in `src/stores/persistMigration.ts` as cleanup-on-load guards. They are **correct migration code**, not dead code — they exist to handle saves written before the field was removed. No action needed.

| Migrated-away field | Removed in |
|--------------------|-----------|
| `permanentInitBonus` / `delveInitBonus` / nested `init` | v6→v7 (initiative removed) |
| `character.legendaryBonuses` | v9→v10 (legendaries became effect-only) |
| `character.attunementSlotsBonus` | v10→v11 (slot cap removed) |
| `resources.rageUsesRemaining` | v10→v11 (Rage is now per-combat) |
| `unlockedUpgrades['pinchpurse-insurance']` | v3→v4 (consolidated into coin-in-pocket) |
| `unlockedUpgrades['sages-pact']` | v10→v11 (attunement upgrade deleted with cap) |

---

## Section H — Checked and Confirmed Live

Items from the brief that turned out to be alive:

| Item | Why it's live |
|------|--------------|
| Monster sprites (all 68 SVGs) | Every file is referenced in src/ by monster `defId` string lookups |
| Boss monsters (asylum-director, duergar-ilyich, etc.) | Placed via `bossDefId:` in `createDelve.ts` — not via chapter pool `defId` |
| `SlashEffect.tsx` | Imported by `SpellEffect.tsx:14` (multi-line named import — grep for single-line `import.*SlashEffect` missed it) |
| `delveAttackBonus` / `delveSpellAttackBonus` | Read by `playerAttack.ts:122` and `spells/helpers.ts:50`; events can also grant them via `applyEventOutcome.ts:420,424` |
| `requiresHpAtLeast` event field | Still set on some event choices (e.g., Risk fork) |
| `cha_scaled_gold` event effect | Used in 3 event choices (lines 608, 780, 837) |
| `attunement` field in schema | Still in schema; just always `false` in practice — see Section B2 |
| `SIR_BRICK_PRESET` | Used by several `scripts/sim-*.ts` files and `src/sim/crossFeatureStressSim.ts` |
| `CampScene.tsx` | Imported by `CampRoom.tsx` — provides chapter-keyed SVG fire illustrations |
| `misty-step` spell | Learned via level-up picker; bot casts it via `actionPolicy.ts:263` when low-HP |

---

## Summary Counts

| Severity | Count | Items |
|----------|-------|-------|
| DEAD-safe-delete | 2 | EnemyCard.tsx, CharacterCreationScreen.v1.tsx |
| SUSPICIOUS | 4 | sharpen/prayer store paths, attunement field branch, requiresCha schema+code paths, disabled quirks |
| STALE-needs-rewrite | 21 | 21 scripts/sim-*.ts (listed in Section D) |
| Stale src/sim harnesses | 4 | rogueSim, immortalHypothesisSim, crossFeatureStressSim, stackingAggregatorSim |
